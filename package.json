{
    "scripts": {
        "build": "cd backend && npm i && npm run build &&",
        "start": "cd backend && npm run start",
        "heroku-postbuild":"cd frontend && npm install && npm run build && npm run start cd ../backend && npm i && npm run build"
    }
}
