{
    "scripts": {
        "build": "cd backend && npm i && npm run build && cd ../frontend && npm install && npm run build",
        "start": "cd backend && npm run start && cd ../frontend && npm run start"
    }
}